import { CartItemOriginalPricePipe } from './cart-item-original-price.pipe';

describe('CartItemOriginalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CartItemOriginalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
