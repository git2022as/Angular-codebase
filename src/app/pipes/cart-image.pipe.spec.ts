import { CartImagePipe } from './cart-image.pipe';

describe('CartImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CartImagePipe();
    expect(pipe).toBeTruthy();
  });
});
