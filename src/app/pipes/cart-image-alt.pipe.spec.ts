import { CartImageAltPipe } from './cart-image-alt.pipe';

describe('CartImageAltPipe', () => {
  it('create an instance', () => {
    const pipe = new CartImageAltPipe();
    expect(pipe).toBeTruthy();
  });
});
