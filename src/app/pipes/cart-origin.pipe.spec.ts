import { CartOriginPipe } from './cart-origin.pipe';

describe('CartOriginPipe', () => {
  it('create an instance', () => {
    const pipe = new CartOriginPipe();
    expect(pipe).toBeTruthy();
  });
});
