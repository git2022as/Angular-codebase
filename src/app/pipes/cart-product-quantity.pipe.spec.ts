import { CartProductQuantityPipe } from './cart-product-quantity.pipe';

describe('CartProductQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new CartProductQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
