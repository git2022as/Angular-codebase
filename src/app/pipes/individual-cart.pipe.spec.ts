import { IndividualCartPipe } from './individual-cart.pipe';

describe('IndividualCartPipe', () => {
  it('create an instance', () => {
    const pipe = new IndividualCartPipe();
    expect(pipe).toBeTruthy();
  });
});
