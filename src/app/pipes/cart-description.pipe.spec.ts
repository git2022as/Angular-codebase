import { CartDescriptionPipe } from './cart-description.pipe';

describe('CartDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CartDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
